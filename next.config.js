/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      unoptimized: true, // 可选：如果不打算使用 Image Optimization
    },
};
  
module.exports = nextConfig;
  